# Exports

**AI_READ_ACCESS: READ_ONLY**
**CONTENT_CLASS: GENERATED_OUTPUT**

This directory contains generated deliverables derived from authoritative content and project-specific work.

Canonical export areas:

- `pdf/`
- `docx/`
- `reports/`

Preserved legacy root-level export content was moved into:

- `legacy_root/`

Use exports for deliverables, not as the source of truth. The authoritative source remains `../rag/`.
